import { Participant, ConnectionQuality } from './types';

export const APP_NAME = "Connect Meet";

// Empty initially, populated by real PeerJS connections
export const MOCK_PARTICIPANTS: Participant[] = [];

export const REACTIONS_LIST = ['👍', '👏', '❤️', '😂', '😮', '🎉'];